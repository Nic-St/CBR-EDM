{
  "name": "cbredm",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply c_edm_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply c_edm_db --remote",
    "db:seed:local": "wrangler d1 execute c_edm_db --local --file=./seed/seed.sql"
  },
  "devDependencies": {
    "fontkit": "^2.0.4",
    "wrangler": "^4.131.0"
  },
  "dependencies": {
    "postal-mime": "^3.0.0"
  }
}
